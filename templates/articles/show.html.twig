{% extends 'base.html.twig' %}

{% block title %}{{ article.title }}{% endblock %}

{% block body %}
  {{ parent() }}
  <h1>{{ article.title }}</h1>
  <p>{{ article.body }}</p>
  <hr>

  {% include 'comment/add_comment.html.twig' %}
  {% include 'comment/show_comments.html.twig' %}
  <a href="/">Go Back</a>
{% endblock %}
